name: TodayPick Single-First Register 260926 f30
on:
  push:
    branches: [master]
    paths:
      - '.github/workflows/todaypick-register-260926-f30.yml'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: todaypick-register-260926-f30
  cancel-in-progress: false

jobs:
  register:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: todaypick-image-publish
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOY_SA }}
          create_credentials_file: true

      - uses: google-github-actions/setup-gcloud@v2

      - name: Register from clean Git master
        shell: bash
        run: |
          set -euo pipefail
          project="my-youtube-automation-497504"
          zone="us-central1-a"
          instance="Admin@tt-orchestra"
          cmd='set -euo pipefail; tmp=/tmp/todaypick-register-260926-f30-clean; rm -rf "$tmp"; git clone --depth 1 --branch master https://github.com/johnpark236-tech/todaypick-web.git "$tmp"; cd "$tmp"; /opt/todaypick-web/.venv/bin/python automation/daily_looks/scripts/cloud_single_first_watcher.py --date 260926 --once; /opt/todaypick-web/.venv/bin/python automation/daily_looks/scripts/cloud_single_first_watcher.py --date 260926 --once'
          gcloud compute ssh "$instance" --project="$project" --zone="$zone" --tunnel-through-iap --command="$cmd"

      - name: Report
        run: echo "TodayPick 260926 f_30 registration command completed"
